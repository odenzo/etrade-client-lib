
etrade {
  sandbox {
    url = "https://apisb.etade.com/"
    key = ${ETRADE_SANDBOX_KEY}
    secret = ${ETRADE_SANDBOX_SECRET}
  }

  prod {
    url = "https://api.etrade.com/"
    key = ${ETRADE_PROD_KEY}
    secret = ${ETRADE_PROD_SECRET}
  }
}